---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";
import alexPhoto from "../../public/alexPhoto.jpg";

type Social = { label: string; url: string };
type Project = {
  title: string;
  summary: string;
  href?: string;
  tags?: string[];
};
type Props = {
  name?: string;
  role?: string;
  avatar?: ImageMetadata;
  email?: string;
  socials?: Social[];
  projects?: Project[];
};

const {
  name = "Alejandro Lera López",
  role = "Software Engineer",
  avatar = alexPhoto,
  email = "lera2466@gmail.com",
  socials = [
    { label: "GitHub", url: "https://github.com/lera2466" },
    {
      label: "LinkedIn",
      url: "https://www.linkedin.com/in/alejandro-lera-lopez-981972304/",
    },
    { label: "Email", url: `mailto:lera2466@gmail.com` },
  ],
  projects = [
    {
      title: "Project 1",
      summary:
        "Refactor de componentes, code-splitting y lazy-loading. LCP < 1.7s en móvil.",
      href: "#",
      tags: ["Perf", "Astro", "React"],
    },
    {
      title: "Project 2",
      summary:
        "Sistema de diseño accesible con tokens, documentación y pruebas visuales.",
      href: "#",
      tags: ["A11y", "Design System", "Docs"],
    },
    {
      title: "Project 3",
      summary: "Utilidades para mejorar la DX (lint, test e2e, CI).",
      href: "#",
      tags: ["DX", "Tooling"],
    },
  ],
} = Astro.props;
---

<main class="home" aria-label="Página principal del portfolio">
  <div class="container">
    <!-- HERO -->
    <section class="hero" aria-labelledby="hero-title">
      <div class="hero__copy">
        <h1 id="hero-title" class="title">
          Hola, soy <span class="accent">{name}</span>.<br />
          Diseño y desarrollo soluciones digitales
          <span class="accent">rápidas</span>,
          <span class="accent">accesibles</span> y
          <span class="accent">sostenibles</span>.
        </h1>
        <br />
        <p class="lead">
          {role}. En este portfolio encontrarás proyectos, aprendizajes y cómo
          puedo aportar valor a nuevos retos.
        </p>

        <div class="actions">
          <a class="btn btn--primary" href={`mailto:${email}`}>Escríbeme</a>
          <a class="btn btn--ghost" href="#projects">Ver proyectos</a>
        </div>

        <ul class="social" aria-label="Perfiles sociales">
          {
            socials.map((s) => (
              <li>
                <a
                  class="chip"
                  href={s.url}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  {s.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <figure class="hero__figure">
        <Image
          src={avatar}
          alt={`Retrato de ${name}`}
          width={256}
          height={256}
          format="jpg"
          class="avatar"
        />
        <figcaption class="sr-only">Foto de {name}</figcaption>
      </figure>
    </section>

    <!-- PROYECTOS -->
    <section id="projects" class="section" aria-labelledby="projects-title">
      <header class="section__header">
        <h2 id="projects-title" class="section__title">Proyectos destacados</h2>
        <p class="section__subtitle">Selección curada de trabajo reciente.</p>
      </header>

      <div class="grid">
        {
          projects.map((p) => (
            <article class="card">
              <div class="card__content">
                <h3 class="card__title">{p.title}</h3>
                <p class="card__summary">{p.summary}</p>
                {p.tags && (
                  <ul class="tags" aria-label="Etiquetas del proyecto">
                    {p.tags.map((t) => (
                      <li class="tag">{t}</li>
                    ))}
                  </ul>
                )}
              </div>
              {p.href && (
                <a class="card__link" href={p.href}>
                  Ver caso →
                </a>
              )}
            </article>
          ))
        }
      </div>
    </section>

    <!-- SOBRE MÍ -->
    <section id="about" class="section" aria-labelledby="about-title">
      <header class="section__header">
        <h2 id="about-title" class="section__title">Sobre mí</h2>
      </header>
      <article class="card card--soft">
        <p>
          Me gusta mucho el mundo de la tecnología, en especial la programación.
          Aún no tengo muy claro qué me gusta más, si el front o el back, así
          que aquí estoy tocando ambos.
        </p>
      </article>
    </section>

    <!-- CONTACTO -->
    <section id="contact" class="section" aria-labelledby="contact-title">
      <header class="section__header">
        <h2 id="contact-title" class="section__title">¿Hablamos?</h2>
      </header>
      <article class="callout">
        <p>
          Escríbeme a <a class="link" href={`mailto:${email}`}>{email}</a> o conéctame
          por LinkedIn/GitHub.
        </p>
        <div class="callout__actions">
          <a class="btn btn--primary" href={`mailto:${email}`}>Contactar</a>
          <a class="btn btn--ghost" href="#projects">Ver proyectos</a>
        </div>
      </article>
    </section>
  </div>
</main>

<style>
  /* ========= THEME (encapsulado) ========= */
  .home {
    --bg: #0c0f17;
    --surface: #121727;
    --surface-2: #141b31;
    --text: #e8ebf4;
    --muted: #a5adc1;
    --border: #1d2540;
    --accent: #7c5cff;
    --accent-2: #22d3ee;
    --radius: 16px;
    --shadow: 0 10px 28px rgba(0, 0, 0, 0.28);
    font-family:
      ui-sans-serif,
      system-ui,
      -apple-system,
      Segoe UI,
      Roboto,
      Inter,
      Arial,
      "Apple Color Emoji",
      "Segoe UI Emoji";
    color: var(--text);
  }
  @media (prefers-color-scheme: light) {
    .home {
      --bg: #f7f9ff;
      --surface: #ffffff;
      --surface-2: #f3f6fd;
      --text: #0c0f14;
      --muted: #5c6784;
      --border: #e5e8f3;
      --shadow: 0 10px 22px rgba(0, 0, 0, 0.08);
    }
  }

  /* Fondo decorativo SOLO en esta sección */
  .home {
    position: relative;
  }
  .home::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    background: radial-gradient(
        1200px 600px at 85% -10%,
        color-mix(in oklab, var(--accent) 12%, transparent),
        transparent 60%
      ),
      radial-gradient(
        1200px 600px at 10% 110%,
        color-mix(in oklab, var(--accent-2) 16%, transparent),
        transparent 60%
      ),
      var(--bg);
  }

  /* ========= RESETS locales ========= */
  .home a {
    color: inherit;
    text-decoration: none;
  }
  .home p {
    margin: 0;
  }
  .home h1,
  .home h2,
  .home h3 {
    margin: 0;
    font-weight: 700;
  }

  /* ========= LAYOUT ========= */
  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 48px 20px 80px;
  }

  /* ========= HERO ========= */
  .hero {
    container-type: inline-size;
    overflow: clip; /* o: hidden */
    align-items: center;
    padding: 32px;
    border-radius: calc(var(--radius) + 6px);
    border: 1px solid var(--border);
    background: linear-gradient(
      180deg,
      color-mix(in oklab, var(--surface) 94%, transparent),
      transparent 100%
    );
    box-shadow: var(--shadow);
  }
  @media (min-width: 900px) {
    .hero {
      grid-template-columns: 1.1fr 0.9fr;
    }
  }

  .title {
    margin-top: 12px;
    margin-bottom: 12px;
    font-size: clamp(1.9rem, 9cqi, 2.75rem);
    line-height: 1.1;
    letter-spacing: -0.02em;
    overflow-wrap: anywhere;
    /* hyphens: auto; */
  }
  .accent {
    background: linear-gradient(
      135deg,
      var(--accent),
      color-mix(in oklab, var(--accent-2) 48%, var(--accent))
    );
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .lead {
    max-inline-size: 62ch;
    margin-top: 10px;
    color: var(--muted);
    font-size: clamp(16px, 2.2vw, 18px);
    max-width: 62ch;
    overflow-wrap: anywhere;
    /* hyphens: auto; */
  }

  .actions {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    margin-top: 18px;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 12px 16px;
    gap: 8px;
    font-weight: 600;
    border: 1px solid var(--border);
    border-radius: 14px;
    background: var(--surface);
    transition:
      transform 0.08s ease,
      border-color 0.2s ease,
      box-shadow 0.2s ease;
  }
  .btn:hover {
    transform: translateY(-1px);
    border-color: color-mix(in oklab, var(--accent) 38%, var(--border));
    box-shadow: var(--shadow);
  }
  .btn--primary {
    color: #fff;
    border-color: transparent;
    background: linear-gradient(
      135deg,
      var(--accent),
      color-mix(in oklab, var(--accent-2) 55%, var(--accent))
    );
  }
  .btn--ghost {
    background: color-mix(in oklab, var(--surface) 96%, transparent);
    color: inherit;
  }
  @media (max-width: 600px) {
    .btn {
      padding: 8px 10px;
    }
  }

  .social {
    display: flex;
    gap: 10px;
    list-style: none;
    padding: 0;
    margin-top: 12px;
    flex-wrap: wrap;
  }
  .chip {
    display: inline-flex;
    align-items: center;
    padding: 8px 12px;
    font-size: 14px;
    color: var(--muted);
    border: 1px solid var(--border);
    border-radius: 999px;
    background: color-mix(in oklab, var(--surface) 96%, transparent);
  }
  .chip:hover {
    border-color: color-mix(in oklab, var(--accent) 30%, var(--border));
  }

  .hero__figure {
    justify-self: center;
    position: relative;
    display: grid;
    place-items: center;
    width: auto;
  }

  .avatar {
    display: block;
    margin-inline: auto;
    width: 256px;
    height: 256px;
    border-radius: 50%;
    object-fit: cover;
    border: 6px solid color-mix(in oklab, var(--surface) 85%, transparent);
    box-shadow: 0 8px 40px rgba(0, 0, 0, 0.25);
    background: #ddd;
    position: relative;
    z-index: 1;
  }

  /* ========= SECCIONES ========= */
  .section {
    margin-top: 48px;
  }
  .section__header {
    margin-bottom: 16px;
  }
  .section__title {
    font-size: clamp(22px, 2.8vw, 30px);
  }
  .section__subtitle {
    color: var(--muted);
    margin-top: 4px;
  }

  /* ========= GRID + CARDS ========= */
  .grid {
    display: grid;
    gap: 16px;
    grid-template-columns: repeat(12, 1fr);
  }
  .card {
    grid-column: span 12;
    display: grid;
    gap: 12px;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--surface);
    padding: 18px;
    box-shadow: var(--shadow);
    transition:
      transform 0.08s ease,
      border-color 0.2s ease,
      box-shadow 0.2s ease;
  }
  .card:hover {
    transform: translateY(-2px);
    border-color: color-mix(in oklab, var(--accent) 22%, var(--border));
    box-shadow: var(--shadow);
  }
  .card__title {
    font-size: 18px;
  }
  .card__summary {
    color: var(--muted);
  }
  .card__link {
    font-weight: 600;
    text-decoration: underline;
  }

  .tags {
    display: flex;
    gap: 8px;
    list-style: none;
    padding: 0;
    margin: 4px 0 0;
    flex-wrap: wrap;
  }
  .tag {
    font-size: 12px;
    padding: 6px 10px;
    border: 1px solid var(--border);
    border-radius: 999px;
    color: var(--muted);
    background: color-mix(in oklab, var(--surface-2) 88%, transparent);
  }

  @media (min-width: 820px) {
    .card {
      grid-column: span 6;
    }
    .grid > .card:nth-child(3n) {
      grid-column: span 12;
    }
  }

  /* ========= CALLOUT (contacto) ========= */
  .callout {
    border: 1px solid var(--border);
    background: linear-gradient(
      180deg,
      color-mix(in oklab, var(--surface-2) 92%, transparent),
      transparent 100%
    );
    border-radius: calc(var(--radius) + 4px);
    padding: 20px;
    box-shadow: var(--shadow);
  }
  .callout__actions {
    margin-top: 12px;
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
  }

  /* ========= UTILIDADES ========= */
  .link {
    text-decoration: underline;
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
</style>
